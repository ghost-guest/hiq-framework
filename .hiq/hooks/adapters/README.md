# Project Hook Adapters

Project-local adapter notes or host config evidence can live here. HiQ core remains host-neutral; concrete hosts call `hiq-hook` and leave run evidence under `.hiq/hooks/runs/`.
