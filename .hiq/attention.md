# Attention

- 用 `~/.hiq/bin/codegraph`，不要用 PATH 上的旧包装
- 产品仓初始化请用 `$hiq-init`；宿主安装或 runtime 同步请用 `$hiq-install`
- 中断后续作：先看 `.hiq/session.md`，需要恢复或交接时用 `$hiq-session`
- 施工前确认 owner skill；验收放行前回到 `$hiq-review`
