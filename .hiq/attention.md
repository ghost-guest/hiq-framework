# Attention

- 用 `~/.hiq/bin/codegraph`，不要用 PATH 上的旧包装
- 产品仓初始化请用 `$hiq-init`；宿主安装或 runtime 同步请用 `$hiq-install`
- 默认自动入口：`$hiq-auto`；需要手动单车道时再显式点名 retained skill
- 中断后续作：先看 `.hiq/session.md`，需要恢复或交接时优先回到 `$hiq-auto`
- 施工前确认当前 owner；验收放行前必须回到 `$hiq-review`
